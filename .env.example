# Calendar mode: "local" (default) or "google"
CALENDAR_MODE=local

# Google Calendar settings (only needed if CALENDAR_MODE=google)
GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com
GOOGLE_SERVICE_ACCOUNT_KEY=./service-account.json
